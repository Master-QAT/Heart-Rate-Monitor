# In VS Code Terminal (Ctrl + `)
git status  # See what files changed

git add .   # Stage all changes

git commit -m "Refactor: Reorganize project structure with proper documentation"

git push    # Push to GitHub